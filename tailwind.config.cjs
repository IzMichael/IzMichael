/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{html,js,svelte,ts}'],
    theme: {
        extend: {},
        fontFamily: {
            sans: ['Nunito Sans'],
            round: ['Nunito'],
            work: ['Work Sans'],
            mono: ['Overpass Mono'],
            code: ['Red Hat Mono'],
            serif: ['PT Serif'],
            stnd: ['Roboto'],
            hand: ['Patrick Hand'],
            loop: ['Sacramento'],
            clean: ['Outfit'],
        },
    },
    plugins: [],
}
